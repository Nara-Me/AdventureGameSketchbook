import React from "react";

const Topbar = () => {

  return (
    <div className="top-bar">
      <div className="name">Name of the project</div>
      <button className="play">Play Mode</button>
      <button className="edit">Edit Mode</button>
    </div>
  );
};

export default Topbar;
